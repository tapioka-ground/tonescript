# 曲ファイルの書き方

`songs/*.rhai` の全仕様。**こう書けばこう鳴る**を1か所にまとめてある。

人が手で書いてもいいし、AI（Claude Code / ChatGPT など）にこの文書を渡して
書かせてもいい。そのために、思わせぶりな言い方を避けて、値の形と範囲と
既定値を全部書いてある。

English: [SONGFILE.md](SONGFILE.md)（正本）

確かめ方:

```
tone check <曲名>      読めるか、何ノートになるかを見る
tone render <曲名>     音にする
```

---

## 0. 30秒で分かる最小の曲

```rhai
let BPM = 120;
let SECTIONS = [["A", 2, "p", "k", "m", 1.0]];
let VOICES = #{ lead: #{ ch: 0, patch: "piano" } };
let MELODY = #{
    "1": bar([[4, "C4"], [4, "E4"], [8, "G4"]]),
    "2": bar([[16, "C5"]]),
};
let ARRANGE = #{ "1": ["lead"], "2": ["lead"] };
```

これで「ド・ミ・ソー、ドー」がピアノで鳴る。必須は `BPM` `SECTIONS`
`VOICES` の3つだけ。他は全部省略できる。

---

## 1. いちばん大事な決まり

### 単位は「16分音符いくつ分」

位置も長さも、すべて16分音符を1目盛りとして数える。これを**目盛り**と呼ぶ。

| 音符 | 目盛り |
|---|---|
| 16分 | 1 |
| 8分 | 2 |
| 付点8分 | 3 |
| 4分 | 4 |
| 付点4分 | 6 |
| 2分 | 8 |
| 全音符（4/4の1小節） | 16 |

### 1小節が何目盛りかは拍子で変わる

```
1小節の目盛り = 分子 × 16 ÷ 分母
```

| 拍子 | 1小節 | 1拍 |
|---|---|---|
| 4/4 | 16 | 4 |
| 3/4 | 12 | 4 |
| 2/4 | 8 | 4 |
| 5/4 | 20 | 4 |
| 6/8 | 12 | 2 |
| 7/8 | 14 | 2 |
| 12/8 | 24 | 2 |

**BPM は「拍」を数える。** 6/8 で BPM 120 と書いたら、8分音符が1分間に
120個という意味になる。

### 位置は「曲の頭からの通し」ではなく「小節番号」で指す

`MELODY` や `CHORDS` は小節番号（1から数える）を鍵にした表で書く。
曲の頭から何目盛り目か、を自分で数える必要はない。

例外は `AUTOMATION` だけで、あれは通しの目盛りで指す。

### Rhai の書き方の注意

- 日本語を鍵に使うときは引用符が要る: `#{ "イントロ": ... }`
- 表の鍵に数を使うときも引用符: `#{ "1": ..., "2": ... }`
- `_` は変数名に使えない。`for _round in ...` のように名前を付ける
- 配列の長さは `arr.len()`（`arr.len` ではない）

---

## 2. 使える小道具

曲ファイルの中でだけ使える関数が3つある。

### `bar([[長さ, 音名], ...])`

1小節ぶんの旋律を組み立てる。**合計が1小節ぶんでなければ読み込みで止まる。**
書き間違いが、鳴らす前に見つかる。

```rhai
bar([[4, "C4"], [4, "E4"], [8, "G4"]])   // 4+4+8 = 16。4/4 なら OK
```

休符は音名を `"rest"` にする。

```rhai
bar([[4, "C4"], [4, "rest"], [8, "G4"]])
```

`bar()` を使わず、生の配列 `[[4, "C4"], ...]` で書いてもいい。
どちらでも合計は検算される（拍子に合わせて）。

### `note("A4")`

音名を MIDI ノート番号にする。`A4` = 69、`C4` = 60。
`#` と `b` が使える（`C#4` も `Db4` も同じ 61）。

### `steps(始め, 終わり, 刻み)`

Python の `range` と同じ。終わりは含まない。

```rhai
for i in steps(0, 16, 2) { ... }   // 0, 2, 4, 6, 8, 10, 12, 14
```

**これが .rhai をデータ形式にしなかった理由。** 「2つおきに置く」が
1行で書ける。データで書くと数字の羅列になって、何をしたかったのかが読めない。

---

## 3. 必ず書くもの

### `BPM`（数）

曲の速さ。**20〜400**。範囲外は読み込みで弾かれる。

### `SECTIONS`（配列）

曲の骨組み。上から順に並ぶ。

```rhai
let SECTIONS = [
    // [名前, 小節数, ベース型, ドラムキット, リフ型, 音量, 拍子]
    ["イントロ", 8, "plain", "light", "main", 0.85],
    ["サビ",     8, "octa",  "full",  "high", 1.00],
    ["間奏",     4, "plain", "light", "main", 0.90, [7, 8]],
];
```

| 位置 | 中身 | 範囲 |
|---|---|---|
| 1 | 名前（文字列） | `SECTION_PATCH` などから引くので、ここと綴りを揃える |
| 2 | 小節数 | 1〜512 |
| 3 | ベース型の名前 | `BASS_PATTERNS` に書いた名前 |
| 4 | ドラムキットの名前 | `DRUM_KITS` に書いた名前 |
| 5 | リフ型の名前 | `ARP_PATTERNS` に書いた名前 |
| 6 | ベロシティ倍率 | だいたい 0.6〜1.2。強さ全体に掛かる |
| 7 | 拍子 `[分子, 分母]`（**省略可**） | 分子 1〜32、分母は 1/2/4/8/16。省くと 4/4 |

型やキットの名前が `BASS_PATTERNS` などに無いと、**そのパートは黙る**
（エラーにはならない）。鳴らないときはまずここを疑う。

### `VOICES`（表）

パートごとの音色。

```rhai
let VOICES = #{
    lead:   #{ ch: 0, patch: "supersaw", program: 81, volume: 100,
               label: "主旋律", color: "#ff9f43" },
    drums:  #{ ch: 9, volume: 112, label: "ドラム", color: "#fd79a8" },
};
```

| 鍵 | 中身 | 省略時 |
|---|---|---|
| `ch` | MIDI チャンネル 0〜15。**9 は打楽器** | 必須 |
| `patch` | 自作シンセの音色名（46種。§9） | 無し＝音が出ない |
| `program` | GM 音源の番号 0〜127。MIDI 書き出しに使う | 送らない |
| `volume` | 0〜127 | 100 |
| `label` | 画面と MIDI に出る名前 | パート名 |
| `color` | `#rrggbb` | 灰色 |

**パート名には決まったものがある。** 下の名前にしないと自動生成が効かない。

| パート | 何が鳴るか |
|---|---|
| `lead` | `MELODY` |
| `bass` | `CHORDS` の低音 × `BASS_PATTERNS` |
| `sub` | `CHORDS` の低音の1オクターブ下。1小節まるごと伸ばす |
| `arp` | `CHORDS` の構成音 × `ARP_PATTERNS` |
| `chords` | `CHORDS` の構成音 × `CHORD_PATTERN` |
| `drums` | `DRUM_KITS` と `EXTRA_HITS` |
| `perc` | 同上のうち金物（`closedhat` `openhat` `shaker` `ride` `crash` `reverse`） |
| `vocal` | 何も自動生成しない。画面で描くか MIDI から入れる |
| `fx` | 同上 |

---

## 4. 音を決めるもの

### `MELODY` — 主旋律

小節番号 → その小節の旋律。`lead` が鳴らす。

```rhai
let MELODY = #{
    "1": bar([[3, "A5"], [1, "A5"], [2, "E5"], [2, "A5"], [4, "C6"], [4, "B5"]]),
    "2": bar([[16, "rest"]]),                      // 1小節まるごと休み
    "3": bar([[4, "C5", "ら"], [12, "E5", "らー"]]), // 3つめは歌詞（省略可）
};
```

- 合計は**その小節の目盛り数**ちょうど。違うと読み込みで止まり、
  `3小節目の旋律の合計が 16/12 です（拍子 3/4）` のように言われる
- 曲の小節数を超える小節に書いても止まる
- 強さは 108 を基準に、セクションの音量と `BAR_ACCENT` が掛かる

**書いた通りの音程がそのまま出る。** 移調も和音への吸着もしない
（`TRANSPOSE` を書いたときだけ移調する）。

### `CHORDS` — 和音

小節番号 → 和音。`bass` `sub` `arp` `chords` の材料になる。

```rhai
let Am = ["Am", ["A3", "C4", "E4"], "A2"];
//         ↑表示名  ↑構成音（上物が使う）  ↑低音（bass と sub が使う）
let CHORDS = #{ "1": Am, "2": Am };
```

構成音は何個でもいい（3つでも4つでも）。`arp` は「何番目の音か」で
引くので、順番に意味がある。

進行を回すときは `steps` を使う:

```rhai
let prog = [Am, F, C, G];
let CHORDS = #{};
for i in steps(0, 16, 1) {
    CHORDS["" + (i + 1)] = prog[i % prog.len()];
}
```

### `ARRANGE` — どの小節で何を鳴らすか

小節番号 → 鳴らすパートの配列。**ここに無いパートはその小節で黙る。**

```rhai
let ARRANGE = #{
    "1": ["lead"],                                  // 旋律だけ
    "2": ["lead", "arp"],
    "3": ["lead", "arp", "bass", "sub", "kick", "closedhat"],
};
```

ドラムは**パートではなく打点の名前**で指す（`kick` `clap` `closedhat`
`openhat` `shaker` `rim` など、`DRUM_KITS` に書いた名前）。

`ARRANGE` を書かないと**何も鳴らない**。これが「音が出ない」で一番多い原因。

盛り上がりは「だんだん増やす」で作る:

```rhai
let full = ["lead", "chords", "bass", "sub", "arp",
            "kick", "clap", "closedhat", "openhat", "shaker"];
let ARRANGE = #{
    "1": ["lead"],
    "2": ["lead"],
    "3": ["lead", "arp"],
    "4": ["lead", "arp"],
    "5": ["lead", "arp", "bass", "sub", "kick", "closedhat"],
};
for i in steps(6, 17, 1) { ARRANGE["" + i] = full; }
```

---

## 5. 伴奏の型

### `BASS_PATTERNS` — ベースの刻み

名前 → `[位置, 長さ, ルートからの半音差]` の配列。1小節ぶんを書く。

```rhai
let BASS_PATTERNS = #{
    // 4分で踏む
    plain: [[0, 4, 0], [4, 4, 0], [8, 4, 0], [12, 4, 0]],
    // 根音と1オクターブ上を同時に叩く
    octa: [[0,2,0],[0,2,12],[2,2,0],[2,2,12],[4,2,0],[4,2,12],[6,2,0],[6,2,12],
           [8,2,0],[8,2,12],[10,2,0],[10,2,12],[12,2,0],[12,2,12],[14,2,0],[14,2,12]],
};
```

半音差は `CHORDS` の低音からの差。`12` で1オクターブ上、`7` で5度上、
`-12` で1オクターブ下。強さの基準は 100。

**拍子が短い小節でははみ出したぶんが落ちる。** 3/4（12目盛り）の小節に
`[12, 4, 0]` を書いた型を当てると、その打点は入らない。次の小節へは
食い込まない。

### `ARP_PATTERNS` — リフ

名前 → `[位置, 長さ, 和音の何番目の音, 何オクターブ上]` の配列。

```rhai
let ARP_PATTERNS = #{
    main: [[0,2,0,0],[2,2,1,0],[4,2,2,0],[6,2,0,0],
           [8,2,1,0],[10,2,2,0],[12,2,0,0],[14,2,1,0]],
};
```

「何番目の音」は `CHORDS` の構成音の添字（0 から）。構成音より大きい数を
書いても落ちない（回り込む）。強さの基準は 92。

### `CHORD_PATTERN` — コードの刻み

`[位置, 長さ, 強さ]` の配列。**型の名前は無く、曲に1つだけ。**
構成音を全部同時に鳴らす。

```rhai
let CHORD_PATTERN = [[2, 1, 84], [6, 1, 76], [10, 1, 84], [14, 1, 80]];
```

裏拍（2, 6, 10, 14）に短く置くのが定番。

### `sub` には型が要らない

`sub` は `ARRANGE` に入れるだけで、`CHORDS` の低音を1オクターブ下げて
1小節まるごと伸ばす。強さの基準は 96。

---

## 6. ドラム

### `DRUM_KITS`

キット名 → 打点名 → `[MIDIノート番号, [[位置, 強さ], ...]]`

```rhai
let DRUM_KITS = #{
    light: #{
        kick:      [36, [[0,100],[4,100],[8,100],[12,100]]],
        closedhat: [42, [[2,52],[6,52],[10,52],[14,52]]],
        clap:      [39, []],                      // 空でもいい
    },
    full: #{
        kick:      [36, [[0,126],[4,126],[8,126],[12,126]]],
        clap:      [39, [[4,112],[12,112]]],
        closedhat: [42, [[0,60],[2,60],[4,60],[6,60],[8,60],[10,60],[12,60],[14,60]]],
    },
};
```

**MIDIノート番号で音が決まる。** 打点名は `ARRANGE` で指すための札で、
音そのものは番号で選ばれる。

| 番号 | 音 |
|---|---|
| 35 | 歪んだキック（ハードスタイル） |
| 36 | キック |
| 37 | リムショット |
| 38 | スネア |
| 39 | クラップ |
| 41 / 45 / 48 | タム（低 / 中 / 高） |
| 42 | 閉じたハット |
| 46 | 開いたハット |
| 49 | クラッシュ |
| 51 | ライド |
| 52 | リバースシンバル |
| 70 | シェイカー |
| その他 | ハット |

### `EXTRA_HITS` — キットに依らない一発もの

```rhai
let EXTRA_HITS = #{
    crash: [49, [[0, 112]]],
    fill:  [38, [[8,86],[10,94],[12,104],[14,118],[15,127]]],
};
```

`ARRANGE` にその名前を入れた小節で鳴る。

### `drums` と `perc` の振り分け

打点名が `closedhat` `openhat` `shaker` `ride` `crash` `reverse` のものは
`perc` レーンへ、それ以外は `drums` レーンへ入る。音量とミックスを
別々にするため。`ARRANGE` には打点名を書けばよく、レーンは意識しなくていい。

---

## 7. 変化を付ける

### `TEMPO_MAP` — テンポの節目

小節番号 → BPM。節目のあいだは連続で補間される。

```rhai
let TEMPO_MAP = #{ "1": 150, "33": 174, "41": 190, "49": 190, "52": 154, "53": 150 };
let TEMPO_CURVE = "smooth";   // "smooth"（既定）か "linear"
```

- `smooth` は両端の傾きが 0 の曲線。そっと動き出してそっと収まる
- `linear` は直線。着地で傾きが一段落ちるので「角」が出る

**急に止めるより、数小節かけて落とすほうが次の全開が効く。**

### `SWING` — 目盛りから外す

ここに書いたものは全部、目盛りにぴったり乗る。打ち込みの音楽にはそれが
正しいが、それ以外の多くではハネが要る。ハネは、2つで1組にして前を伸ばし、
後ろを縮める。

```rhai
let SWING = 0.6;      // 0 で均等、1 で三連符
let SWING_GRID = 2;   // 2 で8分（既定）、4 で4分、1 で16分
```

**音符は動かさない。** 書いた譜面はそのままで、変わるのは目盛り1つの
長さのほう。だから上げ下げして聞き比べても、音符を戻す作業が要らない。

1組の合計は変わらないので、**曲は伸びも縮みもしない**し、小節の頭も
ずれない。他のパートとも合ったまま。

0.5〜0.7 くらいが普通のシャッフル。1.0 は完全な三連符。

### `TRANSPOSE` — 移調

小節番号 → 半音いくつ上げるか。**ドラムには掛からない。**

```rhai
let TRANSPOSE = #{};
for i in steps(53, 69, 1) { TRANSPOSE["" + i] = 2; }   // ラスサビで全音上げ
```

### `BAR_ACCENT` — 小節ごとの強さ

小節番号 → 倍率。8小節すべて同じ強さだと平らに聞こえる。

```rhai
let BAR_ACCENT = #{
    "9": 1.00, "10": 0.90, "11": 0.96, "12": 1.04,   // 一度引いて溜める
    "13": 1.06, "14": 0.96, "15": 1.04, "16": 1.14,  // 開ける
};
```

### `SECTION_PATCH` / `LEAD_PATCH` — セクションで楽器を変える

```rhai
let SECTION_PATCH = #{
    "イントロ": #{ lead: "koto", arp: "koto", bass: "sub", chords: "strings" },
    "サビ":     #{ lead: "hardlead", bass: "hardbass" },
};
let LEAD_PATCH = #{ "イントロ": "piano" };   // lead だけを変える近道
```

優先順位は `SECTION_PATCH` → `LEAD_PATCH` → `VOICES.patch`。

### `AUTOMATION` — 線で動かす

パート → 動かすもの → `[[位置, 値], ...]`。**位置だけは通しの目盛りで書く**
（小節ではない）。節と節のあいだは直線で繋ぐ。

```rhai
let AUTOMATION = #{
    lead: #{
        pan:  [[0, -1.0], [256, 1.0]],            // 左から右へ
        gain: [[0, 1.0], [128, 1.0], [256, 0.25]], // 後半でフェード
    },
};
```

| 動かすもの | 範囲 | 既定 |
|---|---|---|
| `gain` | 0〜8 | 1.0 |
| `pan` | -1〜1（-1が左） | 0（中央） |
| `reverb` | 0〜2 | `MIX` の値 |
| `duck` | 0〜2 | `MIX` の値 |

範囲外や曲の終わりより後の位置は読み込みで弾かれる。

---

## 8. ミックスと仕上げ

```rhai
let GAINS = #{ lead: 1.55, chords: 1.60, bass: 0.90, arp: 1.00,
               drums: 1.55, perc: 1.20, fx: 1.40, vocal: 2.30, sub: 1.60 };
let MASTER_GAIN = 1.0;

let MIX = #{
    lead:   #{ width: 1.35, reverb: 0.26, duck: 0.55 },
    bass:   #{ width: 0.00, reverb: 0.02, duck: 1.00 },
    drums:  #{ width: 0.45, reverb: 0.08, duck: 0.00 },
};

let SIDECHAIN = [0.70, 0.003, 0.020, 0.200];   // [深さ, アタック秒, 保持秒, 戻り秒]
let KICK = #{ weight: 1.15, body: 1.20, click: 0.85, length: 1.15, tail_hz: 78.0 };
let REVERB = [1.9, 4.2];                        // [残響の秒数, 広がり]
let MASTER_LUFS = -9.0;                         // 仕上がりの音圧
let PREMIX_LUFS = -20.0;
```

| `MIX` の鍵 | 意味 |
|---|---|
| `width` | 左右の広がり。0 が完全中央。**低音を広げると芯がぼやける**ので `bass` と `sub` は 0 に |
| `eq` | 音の整え。低・中・高の3つ（下に） | 素通し |
| `reverb` | 残響へ送る量。0 で送らない。0.2〜0.4 が目安 |
| `duck` | サイドチェインの掛かり具合。キックのたびに凹む量 |

### `eq` — 片方を削って、もう片方に場所を空ける

ミックスは音量だけでは作れない。ベースとキックは同じ低い所で鳴るので、
フェーダーをどう動かしても互いを覆い合う。**片方を削って、もう片方に
場所を空ける**のがやり方。

```rhai
let MIX = #{
    bass:  #{ eq: #{ low: 2.0, mid: -3.0 } },
    drums: #{ eq: #{ low: -4.0, high: 3.0 } },
};
```

| 鍵 | 中身 | 既定 |
|---|---|---|
| `low` | `low_hz` から下を何 dB。-24〜24 | 0 |
| `mid` | `mid_hz` のあたりを何 dB | 0 |
| `high` | `high_hz` から上を何 dB | 0 |
| `low_hz` | 低い棚の境い目 | 200 |
| `mid_hz` | 中の山の中心 | 1000 |
| `mid_q` | 中の山の細さ 0.2〜12 | 0.9 |
| `high_hz` | 高い棚の境い目 | 4000 |

3つで足りる。本職の卓は6も8も積んでいるが、実際に使うのは
「下を削る」「中を押す／引く」「上を足す」がほとんど。

**上げるより削るほうが効く。** ある楽器が暗いと感じたら、まずその上に
乗っている楽器の中域を削ってみる。上げたぶんは全部、天井までの余裕を
食っていく。

### 残響の効かせ方

`REVERB` は `[秒数, 広がり]`。秒数は残りが消えるまでのおよその長さ
（0.05〜20）、広がりは 0 で完全に中央、8 でいちばん開く。

**全パートが同じ部屋へ送る。** パートごとに別の残響を掛けるのではなく、
1つの部屋へまとめて送って、戻ってきたぶんを足す。実際の部屋と同じ形で、
そのほうが馴染む（別々に掛けると、パートごとに違う場所で鳴っているように
聞こえる）。

送る量は `MIX` の `reverb` で決める。**0 なら1滴も送らない**ので、
どのパートも 0 なら残響の計算自体が走らない。

```rhai
let REVERB = [2.5, 5.0];
let MIX = #{
    lead:   #{ width: 1.35, reverb: 0.26, duck: 0.55 },
    // 低音は送らない。残響を掛けると輪郭が消えて濁るだけ
    bass:   #{ width: 0.00, reverb: 0.02, duck: 1.00 },
    sub:    #{ width: 0.00, reverb: 0.00, duck: 1.00 },
};
```

長い残響ほど高い音が先に消えるようにしてある。実際の部屋もそうなっている。

`SIDECHAIN` は EDM の「ポンプ感」の正体。キックのたびに他を凹ませ、
戻る途中で次のキックが来る。深く・戻りを長くするほどうねる。

`MASTER_LUFS` の目安: 配信は -14、CD は -9 あたり。

### `AUDIO_TRACKS` — 外で作った歌

```rhai
let AUDIO_TRACKS = #{
    main:    #{ path: "Vocal/main.wav",    gain: 1.00, label: "メイン" },
    double:  #{ path: "Vocal/double.wav",  gain: 0.85, label: "重ね" },
    harmony: #{ path: "Vocal/harmony.wav", gain: 0.42, label: "ハモリ",
                at: 64, trim_in: 0.20, fade_out: 1.5 },
};
```

| 鍵 | 中身 | 省略時 |
|---|---|---|
| `path` | `TONESCRIPT_ROOT` からの相対。絶対パスは弾く | 必須 |
| `gain` | 0 にすれば読まない | 1.0 |
| `label` | 画面に出る名前 | 鍵の名前 |
| `at` | 何目盛り目から鳴らすか。0 は曲の頭 | 0 |
| `trim_in` | 頭を何秒落とすか（息継ぎ、椅子の音） | 0 |
| `trim_out` | 尻を何秒落とすか | 0 |
| `fade_in` | 何秒かけて入るか | 0 |
| `fade_out` | 何秒かけて消えるか | 0 |

- **48kHz・16/24bit か 32bit 小数・ステレオかモノラル**。違うとエラー
- 秒は 0〜600。`at` は目盛りなので、テンポが変われば付いてくる
- 同じパートの別テイクを重ねると**ダブリング**になって太くなる
- 使わないものは `gain: 0.0` にする
- 歌には伴奏側のサイドチェインも尖り制限も掛からない

### `SCALE` / `SCALE_ROOT`

音階。平行声部を作るときに使う（主音からの半音）。

```rhai
let SCALE = [0, 2, 3, 5, 7, 8, 10];   // 自然短音階
let SCALE_ROOT = 9;                    // A のピッチクラス（C=0, A=9）
```

---

## 9. 使える音色 135 種

`tone patches` で一覧が出る。2種類あるが、使い方は同じで `VOICES` に名前を
書くだけ。

**合成の音色（46種）** — Rust で書いて手で詰めたもの。

| 系統 | 名前 |
|---|---|
| リード | `supersaw` `hardlead` `brightsaw` `squarelead` `pluck` `stab` `crystal` `brass` |
| 鍵盤・弦 | `piano` `harpsi` `organ` `strings` `choir` |
| ベース | `acid` `sub` `reese` `fmbass` `donk` `808` `growl` `hardbass` `fingerbass` `upright` `rumble` `wobble` |
| 打・金属 | `bell` `steelpan` `marimba` `kalimba` `gamelan` `santur` |
| 和・アジア | `koto` `shamisen` `shakuhachi` `sinobue` `erhu` |
| 笛・管 | `flute` `quena` `bansuri` `ocarina` `whistle` `panflute` `duduk` `didge` |
| 撥弦 | `sitar` `oud` |


**基本の楽器（89種）** — 作り方を数で書いたもの（§10）。曲ファイルへ写して
直せる。

| 系統 | 名前 |
|---|---|
| 撥弦 | `guitar` `nylon` `eguitar` `distguitar` `twelvestring` `ukulele` `banjo` `mandolin` `balalaika` `bouzouki` `charango` `harp` `kora` `dulcimer` `pizzicato` |
| ベース | `ebass` `slapbass` `fretless` |
| 弓 | `violin` `viola` `cello` `contrabass` `tremolostrings` `kokyu` |
| 和楽器 | `kotostring` `shamisenstring` `biwa` `sanshin` `koto12` `sho` `hichiriki` |
| アジア | `guzheng` `pipa` `sitarstring` |
| 鍵盤 | `rhodes` `wurli` `clav` `celesta` `musicbox` `toypiano` `harmonium` `leslie` `melodica` `accordion` `harmonica` |
| 音程のある打 | `vibraphone` `glocken` `xylophone` `tubularbell` `handpan` `steeldrum` `logdrum` `woodblock` `timpani` `taiko` |
| 金管 | `trumpet` `mutetrumpet` `trombone` `horn` `tuba` |
| 木管 | `sax` `clarinet` `oboe` `bassoon` `piccolo` `recorder` `bagpipe` |
| 声 | `aah` `ooh` `hum` `whisper` |
| パッド | `warmpad` `glasspad` `choirpad` `sweeppad` `stringmachine` `bellpad` |
| シンセ | `plucksynth` `bellsynth` `pwmlead` `hoover` `organbass` `clickbass` |
| チップチューン | `nespulse` `nesthin` `neslead` `nestri` `nesarp` `gameboy` |

**どこまで本物に近いか、正直に。** 撥弦は弦そのものを真似ている（波が弦を
往復し、端で折り返すたびに高い音から失われる）ので、単体で聞いても持つ。
鍵盤と金属はよく似る。倍音の並びがもともと単純だから。管は「それらしい」
止まりで、本物は息と唇と管の相互作用で鳴るが、そこは真似ていない。
弓が一番遠い。

生ドラム・生ピアノ・オーケストラは**そもそもこの方法では作れない**。
音源ライブラリが何十GBあるのはそのため。

合成の音色の選び方の目安:

- **速い曲の主旋律** — `supersaw` `hardlead` `brightsaw`。倍音が多くて抜ける
- **静かな所** — `piano` `crystal` `strings`。`crystal` は伸ばすと澄む
- **低音の土台** — `sub`（40〜90Hz を支える）。その上で `acid` や `reese` が刻む
- **和風** — `koto`（撥弦で粒が立つ）`shakuhachi`（息が主役）`shamisen`（サワリのビビり）
- **息もの** — 立ち上がりが遅い。速い刻みには向かない

音色によっては**余韻**が付く（音価より長く鳴る）。`gamelan` は 1.6 秒、
`piano` は 0.42 秒、`808` は 0.70 秒。刻んだときに繋がって聞こえるのはこれ。

---

---

## 10. 自分で音色を作る

46種で終わりではない。`PATCHES` に数を書けば、それがそのまま楽器になる。
`VOICES` でも `SECTION_PATCH` でも、内蔵の名前と同じように使える。

**同じ名前なら曲ファイル側が勝つ。** 内蔵の `piano` が気に入らなければ、
自分の `piano` を書けばいい。

音が出る最小の書き方：

```rhai
let PATCHES = #{ plain: #{} };
```

全部に既定値がある。変えたい所だけ書く。

### 組み立ての順

```text
  発振器を重ねる ─┬→ 音量のかたち ─→ フィルタ ─→ 歪み ─→ やまびこ ─→ 音量
  倍音を足す     ─┤        ↑              ↑
  FM を足す      ─┤    ビブラート   フィルタのかたち
  頭の雑音を足す ─┘
```

### `osc` — 発振器

何本でも重ねられる（16本まで）。`mix` は互いの比なので、`1.0` と `1.0` なら
半分ずつ。

```rhai
osc: [
    #{ wave: "saw",    mix: 1.0, detune: -14 },
    #{ wave: "saw",    mix: 1.0, detune:  14 },
    #{ wave: "square", mix: 0.5, octave: -1 },
],
```

| 鍵 | 中身 | 既定 |
|---|---|---|
| `wave` | `saw` / `square` / `sine` / `noise` / `string` | `saw` |
| `mix` | どれだけ混ぜるか | 1.0 |
| `detune` | 音程のずれ（セント。100 で半音）。±4800 | 0 |
| `octave` | 何オクターブ上下させるか。±4 | 0 |

**のこぎり2本を ±10〜20 セントずらして重ねるのが「分厚いリード」の正体。**
それ以上ずらすと、1つの音に聞こえなくなる。

### `env` — 音量のかたち

```rhai
env: #{ a: 0.008, d: 0.20, s: 0.75, r: 0.12 },
```

| 鍵 | 中身 |
|---|---|
| `a` | 立ち上がりの秒数。0.001 は打撃、0.3 は息 |
| `d` | 伸ばす高さまで落ちる秒数 |
| `s` | 伸ばしている間の高さ（0〜1）。**0 にすると減衰して消える** — 撥弦はこれ |
| `r` | 音価が切れてから消えるまでの秒数 |

### `filter` — 上を削る

```rhai
filter: #{ kind: "ladder", base: 700, sweep: 7000, res: 0.35, vel: 1500,
           env: #{ a: 0.004, d: 0.25, curve: 2.2 } },
```

| 鍵 | 中身 | 既定 |
|---|---|---|
| `kind` | `ladder`（=`lowpass`）/ `highpass` / `bandpass` / `none` | 他を書けば `ladder` |
| `base` | いちばん閉じたときの高さ（Hz） | 1200 |
| `sweep` | かたちで開く量（Hz） | 4000 |
| `res` | 響き 0〜0.95。**0.8 を超えると口笛のように鳴き出す** | 0.2 |
| `track` | 音程についてくる量。1.0 で高い音ほど開く | 0 |
| `vel` | 強く弾いたときに開く量（Hz） | 0 |
| `env.a` `env.d` `env.curve` | 開き方のかたち | 0.001 / 0.08 / 2.0 |

**`base` を低く `sweep` を高くすると、定番の「びよん」になる。**
フィルタが一気に開いて、また閉じる。


### `wave: "string"` — 本物の撥弦

他の波は「形」だが、これだけは**弦そのもの**。弦の長さぶんの管に雑音を
入れて往復させ、折り返すたびに高い音を少し失わせる。本物の弦が減衰しながら
だんだん丸くなるのはこれで、「のこぎり波をフィルタで削る」では弦に
聞こえないのもこれが無いから。

```rhai
osc: [#{ wave: "string", decay: 3.2, bright: 0.62, pick: 0.20 }],
```

| 鍵 | 中身 | 既定 |
|---|---|---|
| `decay` | 伸びる秒数。0.5 でミュート、5 で開放弦 | 2.0 |
| `bright` | 0〜1。低いとナイロン弦（すぐ丸くなる）、高いと金属弦（伸びる） | 0.5 |
| `pick` | 弾く位置 0〜1。0.5 が真ん中（丸い）、0.1 でブリッジ寄り（硬い） | 0.25 |

`vibrato` と `fm` は効かない（撥いた弦は揺らせない）。

### `body` — 胴鳴り

`[中心の高さ, 鋭さ, 混ぜる量]`。ギターらしさの半分は弦ではなく**箱**にある。
無いと弦が痩せて聞こえる。

```rhai
body: [[100.0, 6.0, 0.45], [205.0, 8.0, 0.25], [430.0, 9.0, 0.12]],
```

これはアコースティックギターのおよその値。ウクレレは箱が小さいので山が
高い所（240Hz、520Hz）に来る。アンプのキャビネットは 400Hz 前後の
幅広く緩い山になる。

### `partials` — 整数倍でない倍音

`[音程の倍率, 大きさ, 落ちる秒数]` を直に足す。本物の鐘や銅鑼は倍音が
**整数倍になっていない**。だからのこぎり波と違う音に聞こえる。

```rhai
osc: [],
partials: [[1.0, 0.5, 2.4], [2.76, 0.28, 1.6], [5.40, 0.16, 1.0]],
```

この 1 / 2.76 / 5.4 という数は、教会の鐘のおおよその比。

### `attack` — 頭の雑音

```rhai
attack: #{ amount: 0.22, hp: 2500, a: 0.0003, d: 0.006 },
```

弦を弾く爪、笛の息の当たり。`hp` より下を落とすので、低音を太らせずに
上へ乗る。

### `fm` — 金属質

```rhai
fm: #{ ratio: 3.5, index: 6.0, decay: 0.4 },
```

片方の波でもう片方の音程を揺らす。`ratio` を整数（2、3）にすると音楽的に、
小数（3.5、5.7）にすると金属的になる。`index` はどれだけ揺らすか。

### `vibrato` / `delay` / `drive` / `gain` / `ring`

```rhai
vibrato: #{ rate: 5.2, depth: 0.008, delay: 0.3 },  // 毎秒何回 / 深さ 0.01 ≒ ±17セント
delay:   #{ time: 0.12, feedback: 0.4, mix: 0.3 },
drive: 1.6,   // 1.0 で掛けない。2 を超えるとはっきり歪む
gain: 0.55,   // 最後の音量
ring: 0.18,   // 音価より長く鳴る秒数
```

**`ring` が「刻んだときに繋がって聞こえる」正体。** 鐘なら 1〜2秒、
ピアノで 0.4秒、リードは 0。

### 写して直せる見本

```rhai
let PATCHES = #{
    // 分厚いリード
    fatsaw: #{
        osc: [#{ wave: "saw", detune: -14 }, #{ wave: "saw", detune: 14 },
              #{ wave: "saw", mix: 0.6, octave: -1 }],
        env: #{ a: 0.008, d: 0.20, s: 0.75, r: 0.12 },
        filter: #{ base: 700, sweep: 7000, res: 0.35, vel: 1500 },
        drive: 1.6, gain: 0.55,
    },
    // ガラスの鈴
    glassbell: #{
        osc: [],
        partials: [[1.0, 0.5, 2.4], [2.76, 0.28, 1.6], [5.40, 0.16, 1.0]],
        env: #{ a: 0.001, d: 2.5, s: 0.0, r: 0.6 },
        gain: 1.2, ring: 1.8,
    },
    // 撥いた弦
    pickedstring: #{
        osc: [#{ wave: "saw", mix: 0.6 }, #{ wave: "square", mix: 0.4 }],
        env: #{ a: 0.001, d: 0.45, s: 0.0, r: 0.10 },
        filter: #{ base: 1400, sweep: 6000, res: 0.30 },
        attack: #{ amount: 0.22, hp: 2500, a: 0.0003, d: 0.006 },
        ring: 0.18,
    },
    // 息もの。雑音を狭い帯で削ると笛になる
    airy: #{
        osc: [#{ wave: "noise" }],
        filter: #{ kind: "bandpass", base: 1200, res: 0.6 },
        env: #{ a: 0.08, d: 0.2, s: 0.8, r: 0.15 },
        vibrato: #{ rate: 5.2, depth: 0.008, delay: 0.3 },
        gain: 1.5,
    },
    // 低音の土台
    deepsub: #{
        osc: [#{ wave: "sine" }, #{ wave: "sine", mix: 0.3, octave: -1 }],
        env: #{ a: 0.004, d: 0.1, s: 0.9, r: 0.08 },
        filter: #{ base: 140, sweep: 0, res: 0.1 },
        gain: 1.0,
    },
};
```

### 読み込みで見ていること

おかしい数は理由付きで止まる。**鳴らす前に分かるように。**

- 音の素が1つも無い（`osc` が空で `partials` も `attack` も無い）
- `mix` が全部 0
- `res` が 0.95 より上（音ではなく発振になる）
- `env` の秒数が 0〜30 の外、`drive` が 0.1〜20 の外
- 知らない `wave` や `kind` — 使える名前を並べて言う

`tone check <曲>` で全部走る。

### AI に音を作らせる

この節がそのまま渡す文書になる。言葉で頼めばいい。

> `SONGFILE.ja.md` に従って、`PATCHES` に `icepad` を足して。
> 冷たくて、1秒くらいかけて開く、息っぽいパッド。

そのあと `tone render <曲>` で聞く。音作りは「まず作って直す」なので、
「もっと硬く」「暗すぎる」と言えば、AI はここの数を動かせる。

## 11. よくある詰まり

| 症状 | 原因 |
|---|---|
| **音が出ない** | `ARRANGE` にそのパートを入れていない |
| ベース／リフが鳴らない | `SECTIONS` の型名が `BASS_PATTERNS` などに無い |
| 旋律が鳴らない | `ARRANGE` に `lead` が無い。または `MELODY` の小節番号が曲の外 |
| 読み込みで止まる | 旋律の合計が1小節ぶんでない。メッセージに `16/12` のように出る |
| 3/4 なのに4拍鳴る | 型が 4/4 前提。はみ出したぶんは落ちるので、拍子に合わせて書く |
| ドラムが変な音 | 打点名ではなく **MIDIノート番号**で音が決まる |
| 日本語の鍵でエラー | 引用符が要る。`#{ "イントロ": ... }` |
| 速さを変えたのに変わらない | `BPM` だけ直して `TEMPO_MAP` が古い |
| 歌が読めない | 48kHz 16bit でない。絶対パスを書いている |

---

## 12. AI に書かせるときの渡し方

この文書をそのまま渡して、こう頼めばいい。

> `SONGFILE.ja.md` の仕様に従って `songs/<名前>.rhai` を書いて。
> 〈どんな曲にしたいか〉

書かせたあと、必ずこれで確かめる:

```
tone check <名前>     読めるか、各パート何ノートになるか
tone render <名前>    実際に音にする
```

`check` が通れば、少なくとも**曲として破綻していない**ことは保証される
（旋律の合計、小節の範囲、値の範囲は読み込みで全部検算される）。

AI に伝えておくと良い点:

- 必須は `BPM` `SECTIONS` `VOICES` の3つだけ。凝る前にまずこれで通す
- `ARRANGE` を書き忘れると**何も鳴らない**
- 旋律の合計は必ず1小節ぶん。`bar()` を使えば書き間違いがその場で出る
- 拍子を変えたら、伴奏の型も拍子に合わせて書き直す
- 音色名は46種のどれか、または `PATCHES`（§10）で自分が作ったもの。
  無い名前を書くと**そのパートが黙る**
- 46種に無い音が欲しいなら、妥協せず**音色を書く**（§10）

---

## 13. 完全な一覧

| 名前 | 必須 | 形 | 既定 |
|---|---|---|---|
| `BPM` | ● | 数 20〜400 | — |
| `SECTIONS` | ● | 配列 | — |
| `VOICES` | ● | 表 | — |
| `TITLE` | | 文字列 | `"untitled"` |
| `KEY` | | 文字列 | `""` |
| `TEMPO_MAP` | | 小節→BPM | `{1: BPM}` |
| `TEMPO_CURVE` | | `"smooth"` / `"linear"` | `"smooth"` |
| `SWING` | | 0〜1 | 0（均等） |
| `SWING_GRID` | | 何目盛りごとにハネるか | 2 |
| `CHORDS` | | 小節→和音 | 無し |
| `MELODY` | | 小節→旋律 | 無し |
| `ARRANGE` | | 小節→パート配列 | 無し（＝何も鳴らない） |
| `TRANSPOSE` | | 小節→半音 | 無し |
| `BAR_ACCENT` | | 小節→倍率 | 1.0 |
| `BASS_PATTERNS` | | 名前→配列 | 無し |
| `ARP_PATTERNS` | | 名前→配列 | 無し |
| `CHORD_PATTERN` | | 配列 | 無し |
| `DRUM_KITS` | | キット名→打点 | 無し |
| `EXTRA_HITS` | | 名前→打点 | 無し |
| `EDIT_PARTS` | | 文字列の配列 | `VOICES` の鍵 |
| `AUDIO_TRACKS` | | 名前→歌（`at` `trim_in` `trim_out` `fade_in` `fade_out`） | 無し |
| `GAINS` | | パート→倍率 | 1.0 |
| `MASTER_GAIN` | | 数 | 1.0 |
| `MIX` | | パート→設定 | 中央・残響無し・ダッキング無し |
| `AUTOMATION` | | パート→線 | 無し |
| `SECTION_PATCH` | | セクション→パート→音色 | 無し |
| `LEAD_PATCH` | | セクション→音色 | 無し |
| `PATCHES` | | 名前→音色の作り方 | 無し |
| `SCALE` | | 半音の配列 | 無し |
| `SCALE_ROOT` | | 数 | 0 |
| `SIDECHAIN` | | `[深さ, ア, 保, 戻]` | `[0.70, 0.003, 0.020, 0.200]` |
| `KICK` | | 表 | 既定のキック |
| `REVERB` | | `[秒, 広がり]` | `[1.9, 4.2]` |
| `MASTER_LUFS` | | 数 | -9.0 |
| `PREMIX_LUFS` | | 数 | -20.0 |
